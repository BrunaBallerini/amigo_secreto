<h1>Lista de carros</h1>

<table class="table">
  <thead>
    <tr>
      <th>id</th>
      <th>Nome</th>
      <th>Modelo</th>
      <th>Ano</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @cars.each do |car| %>
      <tr>
        <th><%= car.id %></th> 
        <th><%= car.name %></th>
        <th><%= car.model %></th>
        <th><%= car.year %></th>
        <th style="width:80px"><a href="<%= cars_show_path(car.id) %>" class="btn btn-primary">Mostrar</a></th> 
<%
=begin%>
 <th><a href="/cars/<%= car.id %>"><%= car.id %></a></th> 
<%
=end%>
      </tr>
    <% end %>
  </tbody>
</table>
